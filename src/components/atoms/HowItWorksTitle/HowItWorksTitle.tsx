const HowItWorksTitle = ({ children }: { children: React.ReactNode }) => {
  return (
    <h6 className="sm:text-[31px] text-[25px] font-bold text-background m-0 leading-tight">
      {children}
    </h6>
  );
};

export default HowItWorksTitle;
